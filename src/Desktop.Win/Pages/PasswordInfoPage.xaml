<Page
    x:Class="Desktop.Win.Pages.PasswordInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Desktop.Win.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="10 0 10 0">
            <TextBlock x:Name="headerBlock" Text = "Добавление информации о пароле"
                       Style="{ThemeResource HeaderTextBlockStyle}" />
            <TextBlock>Название</TextBlock>
            <TextBox x:Name="nameBox" />
            <TextBlock>Логин</TextBlock>
            <TextBox x:Name="loginBox" />
            <TextBlock>Пароль</TextBlock>
            <TextBox x:Name="passwordBox" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Сохранить" Margin="10" Click="SaveOnClick"/>
                <Button Content="Отмена" Margin="10" Click="CancelOnClick" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
